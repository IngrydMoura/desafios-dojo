/**
 * @param {Array<[number, number]>} coins 
 * Coins Ruazinho is using to pay. 
 * It's a tuple, the first value is the quantity of coins (Int).
 * and the second is the coin value (Float).
 * 
 * @param {number} value value to be paid (Float).
 * 
 * @returns {Array<[number, number]>} returns the coins left.
 * It's a tuple, the first value is the quantity of coins (Int).
 * and the second is the coin value (Float).
 */
function challenge(coins, value) {

    return 
}

module.exports = challenge;
